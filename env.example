# Description: Environment variables for the application
# You can use development or production
APP_ENV=production
SERVER_PORT=8080
JWT_SECRET=secret
JWT_ACCESS_TOKEN_EXPIRES=3600

# Database
DB_DRIVER=sqlite
DB_PATH=./writable/db/bank.db
